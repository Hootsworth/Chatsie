{
  "name": "chatsie-root",
  "version": "1.0.0",
  "description": "Root package for Railway deployment",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm run start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
